# Agent Teams Monitor

实时 Web 仪表板，用于监控 Claude Agent Teams 活动。

## 功能特性

### 实时监控
- **团队监控**: 查看所有 Claude Agent Teams 的状态
- **成员状态**: 实时显示 Agent 状态（🟢在线 / 🟡空闲 / ⚫离线）
- **任务跟踪**: 查看任务列表、状态和进度
- **消息查看**: 查看 Agent 之间的消息通信

### Agent 控制
- **▶️ 恢复/播放**: 启动或恢复 Agent 执行
- **⏸️ 暂停**: 暂停 Agent 执行
- **⏻ 关闭**: 请求关闭 Agent

### 实时更新
- WebSocket 驱动，文件变化自动刷新
- 无需手动刷新页面

## 快速开始

### 1. 复制项目模板

```bash
cd ~/fm-skills/agent-teams-monitor
cp -r assets/project-template /path/to/destination
cd /path/to/destination
```

### 2. 安装依赖

```bash
npm run install:all
```

### 3. 启动服务

开发模式（热更新）：
```bash
npm run dev
```

生产模式：
```bash
npm run preview
```

### 4. 访问 Dashboard

- 前端：http://localhost:5173
- 后端 API：http://localhost:3001

## 界面预览

### 三列布局
1. **左侧**: 团队列表（可折叠）
2. **中间**: 成员列表，显示状态指示器
3. **右侧**: 详情面板（成员概览、任务、消息）

### 状态指示器
- 🟢 **Active**: 有进行中的任务或最近 5 分钟内有活动
- 🟡 **Idle**: 有任务但无最近活动
- ⚫ **Offline**: 无分配任务

## 监控内容

从 `~/.claude/` 目录读取数据：
- `~/.claude/teams/` - 团队配置
- `~/.claude/tasks/{team}/` - 任务列表
- `~/.claude/teams/{team}/inboxes/` - Agent 消息
- `~/.claude/debug/` - 调试日志

## 技术栈

- **后端**: Node.js + Express + WebSocket + chokidar
- **前端**: React + Vite + TailwindCSS v4
- **数据源**: 本地文件系统 `~/.claude/`

## 项目结构

```
agent-teams-monitor/
├── SKILL.md              # Skill 元数据
├── README.md             # 本文档
├── assets/
│   └── project-template/ # 完整项目模板
│       ├── backend/      # 后端服务
│       ├── frontend/     # 前端应用
│       └── package.json  # 根目录配置
├── references/
│   ├── ARCHITECTURE.md   # 架构文档
│   └── api-reference.md  # API 文档
└── scripts/
    └── setup.sh          # 安装脚本
```

## 了解更多

- [架构设计](./references/ARCHITECTURE.md)
- [API 参考](./references/api-reference.md)
